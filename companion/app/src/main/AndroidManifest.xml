<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="org.moire.ultrasonic" />
        <intent>
            <action android:name="androidx.media3.session.MediaLibraryService" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault.DayNight">

        <activity
            android:name=".StatusActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Bound by the Pebble phone app. Only holders of RECEIVE_DATA_FROM_WATCH may bind;
             BasePebbleListenerService additionally rejects callers other than the selected app. -->
        <service
            android:name=".UltrasonicPebbleService"
            android:exported="true"
            android:permission="io.rebble.pebblekit2.permission.RECEIVE_DATA_FROM_WATCH">
            <intent-filter>
                <action android:name="io.rebble.pebblekit2.RECEIVE_DATA_FROM_WATCH" />
            </intent-filter>
        </service>
    </application>
</manifest>
